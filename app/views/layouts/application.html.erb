<!DOCTYPE html>
<html>
  <head>
    <title>PhysisisterWebsite</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js" integrity="sha384-smHYKdLADwkXOn1EmN1qk/HfnUcbVRZyYmZ4qpPea6sjB/pTJ0euyQp0Mk8ck+5T" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.bundle.min.js" integrity="sha384-u/bQvRA/1bobcXlcEYpsEdFVK/vJs3+T+nXLsBYJthmdBuavHvAW6UsmqO2Gd/F9" crossorigin="anonymous"></script>
    <link href="https://fonts.googleapis.com/css?family=Playfair+Display|Quicksand|Roboto+Slab" rel="stylesheet">
    <style>
      body {
        margin: 0;
      }
      .article-show {
        padding-bottom: 100px;
      }
      .textarea {
        height: 500px;
        width: 700px;
      }
      a:link, a:visited, a:hover {
        text-decoration: none;
        color: black;
      }
      .comment-form a:link, .comment-form a:visited {
        text-decoration: none;
        color: black;
      }
      .comment-form a:hover, .comment-form a:active {
        text-decoration: none;
        color: blue;
      }
      .list-group ul {
        margin: 0;
        padding: 0;
        list-style: none;
      }
      .reply {
        padding-left: 20px;
      }
      .profile-nav {
        text-align: right;
        padding-right: 10px;
      }
      header {
        background-color: white;
      }
      #physisister-title-div {
        padding: 220px;
        background: url("/nebula.jpg");
        min-height: 500px;
        text-align: center;
        background-attachment: fixed;
        background-position: center;
        background-repeat: no-repeat;
        background-size: 100% 100%;
      }
      #physisister-title {
        color: white;
        font-size: 100px;
        /* font-family: 'Playfair Display', serif; */
        font-family: 'Roboto Slab', serif;
      }
      header::after {
        content: '';
        display: table;
        clear: both;
      }
      nav {
        float: right;
      }
      nav ul {
        margin: 0;
        padding: 0;
        list-style: none;
      }
      nav ul li {
        display: inline-block;
        margin-left: 20px;
        margin-right: 20px;
        padding-top: 25px;
        padding-bottom: 25px;
        position: relative;
        z-index: 1000;
      }
      nav a {
        text-decoration: none;
        text-transform: uppercase;
        font-size: 17px;
        font-weight: 600;
        font-family: 'Quicksand', sans-serif;
      }
      nav a:active, nav a:hover {
        text-decoration: none;
        color: #2865FF;
      }
      nav a::before {
        content: '';
        display: block;
        height: 5px;
        background-color: black;

        position: absolute;
        top: 0;
        width: 0%;
        transition: all ease-in-out 200ms;
      }
      nav a:hover::before {
        width: 100%;
      }
      ul.sub-menu {
        position: absolute;
        background-color: white;
        margin-left: -20px;
        padding-top: -25px;
        padding-bottom: 10px;
        opacity: 0;
      }
      nav li:hover .sub-menu {
        opacity: 1;
      }
      .reply-link {
        cursor: pointer;
      }
      .reply-link:hover, .reply-link:active  {
        color: blue;
      }
      #description-div {
        text-align: center;
        margin: 90px;
      }
      #description-title {
        padding: 25px;
        font-size: 40px;
      }
      #description-para {
        display: inline-block;
        width: 60%;
        font-size: 22px;
      }
      #button-div {
        text-align: center;
        margin: 100px;
      }
      #button-div .btn {
        width: 400px;
        height: 100px;
        font-size: 35px;
        margin: 10px;
        border-color: transparent;
      }
    </style>
  </head>

  <body>

    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>

    <script>
    function myFunction(number) {
      var name = "reply_form_" + number.toString()
      var x = document.getElementById(name);
      if (x.style.display === "none") {
          x.style.display = "block";
      } else {
          x.style.display = "none";
      }
    }
</script>
  </body>
</html>
